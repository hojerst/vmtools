#cloud-config

# upgrade packages on first boot
# package_upgrade: true

# add users
# "default" means: add default distribution's default user (ubuntu)
#
#users:
#  - default
#  - name: username
#    gecos: Full Name
#    lock-passwd: false
#    passwd: passwordhash
#    shell: /bin/bash
#    sudo: ALL=(ALL) NOPASSWD:ALL
#    ssh-authorized-keys:
#      - ssh-rsa xxx

# reboot after first boot (so new packages/kernel will be loaded)
#power_state:
#  mode: reboot

# further configuration can be found at
# http://cloudinit.readthedocs.org/en/latest/topics/examples.html