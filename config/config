# storage pool for disks
POOL=vmdata

# default memory size in mb
DEFAULT_MEMORY=1024

# default disk size in gb
DEFAULT_DISKSIZE=20

# default number of cpus
DEFAULT_VCPUS=1

# image to use for primary hdd
IMAGE=images/ubuntu-14.04.img

# startup script for vm
FIRSTBOOTSCRIPT=firstboot.sh
